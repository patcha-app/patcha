  # Both binaries
  ./tests/manual/raw_outputs.sh

  # Pretty-print JSON
  ./tests/manual/raw_outputs.sh --pretty

  # AX only
  ./tests/manual/raw_outputs.sh --ax-only --pretty

  # OCR only (full-screen capture)
  ./tests/manual/raw_outputs.sh --ocr-only --pretty
