<!--
Thanks for contributing to patcha! Please fill this out so reviewers have the
context they need. Keep it short — bullet points are fine.
-->

## What does this change?

<!-- A one-paragraph summary of the change and why it's needed. -->

## Related issues

<!-- e.g. Closes #123 -->

## How was it tested?

<!-- Commands you ran, platforms you tested on (patcha is macOS-only), and any
manual verification. -->

- [ ] `cargo build` passes (in `rust/`)
- [ ] `cargo clippy --all-targets` is clean
- [ ] `cargo fmt --all` applied
- [ ] `cargo test` passes locally (some collector tests require macOS permissions)

## Checklist

- [ ] Commit messages follow the `type: description` convention (`feat:`, `fix:`, `chore:`)
- [ ] I did not commit secrets or personal activity data (`~/.patcha`, `.env`)
- [ ] Docs updated if behavior or configuration changed
